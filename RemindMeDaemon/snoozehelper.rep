#include <QUuid>

class SnoozeHelper
{
	SLOT(void loadReminder(const QUuid &id, quint32 versionCode))
	SLOT(void snoozeReminder(const QUuid &id, const QString &expression))

	SIGNAL(reminderLoaded(const QUuid &id, const QString &description))
	SIGNAL(reminderSnoozed(const QUuid &id))
	SIGNAL(reminderError(const QUuid &id, const QString &error, bool close))
};
